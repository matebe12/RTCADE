{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Server-only deploy'"
  },
  "deploy": {
    "startCommand": "node --import tsx server/index.ts",
    "healthcheckPath": "/api/roms",
    "restartPolicyType": "ON_FAILURE"
  }
}
